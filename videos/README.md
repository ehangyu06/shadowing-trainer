Keep original movie files here on the Mac. Do not commit them.

The app stores video_id in clip metadata, not this folder path.
GitHub Pages never needs these files. On iPad, use Select Video File instead.
